import { PaymentMethodModel } from './payment-method-model';

describe('PaymentMethodModel', () => {
  it('should create an instance', () => {
    expect(new PaymentMethodModel()).toBeTruthy();
  });
});
