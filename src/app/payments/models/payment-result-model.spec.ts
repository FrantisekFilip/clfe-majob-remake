import { PaymentResultModel } from './payment-result-model';

describe('PaymentResultModel', () => {
  it('should create an instance', () => {
    expect(new PaymentResultModel()).toBeTruthy();
  });
});
