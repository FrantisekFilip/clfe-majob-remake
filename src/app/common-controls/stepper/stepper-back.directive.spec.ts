import { StepperBackDirective } from './stepper-back.directive';

describe('StepperBackDirective', () => {
  it('should create an instance', () => {
    const directive = new StepperBackDirective(null);
    expect(directive).toBeTruthy();
  });
});
